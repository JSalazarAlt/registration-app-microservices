apiVersion: apps/v1
kind: Deployment
metadata:
  name: bffservice
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bffservice
  template:
    metadata:
      labels:
        app: bffservice
    spec:
      containers:
      - name: bffservice
        image: bffservice:latest
        ports:
        - containerPort: 3000
        env:
        - name: AUTH_SERVICE_URL
          value: "http://authservice:8080"
        - name: USER_SERVICE_URL
          value: "http://userservice:8081"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: bffservice
spec:
  selector:
    app: bffservice
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP
